from CommonTypes import *
from numpy import (append, array, concatenate, diag, einsum, hsplit, ndarray,
                   newaxis, row_stack, split, sqrt, stack, sum, vstack, where)
from numpy.linalg import norm
